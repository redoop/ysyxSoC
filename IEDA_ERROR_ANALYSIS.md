# iEDA netlist_opt 崩溃问题分析

## 错误信息

```
ERROR: Command 'iEDA' died with <Signals.SIGABRT: 6>
步骤: netlist_opt (网表优化)
脚本: run_iNO_fix_fanout.tcl
耗时: 0.79秒 (立即崩溃)
```

## 根本原因

### SIGABRT (信号 6) 表示:
- ✗ 工具内部断言失败
- ✗ 内存访问错误
- ✗ 设计超出工具处理能力

### 具体原因分析:

1. **设计规模过大**
   - 47 个模块
   - 735 个 wire
   - 165 个 reg
   - 包含: CPU + 7个外设 + 2个AI加速器

2. **扇出限制**
   - 当前设置: max_fanout = 32
   - 某些信号(如时钟、复位)扇出可能超过此值
   - iEDA 的 fix_fanout 工具无法处理

3. **综合成功但优化失败**
   - Yosys 综合: ✓ 成功 (454秒)
   - iEDA netlist_opt: ✗ 失败 (0.79秒)
   - 说明网表本身没问题,是优化工具的限制

## 解决方案优先级

### 🥇 方案 1: 调整参数 (推荐,最快)

**在线 iEDA 重新提交时修改:**

```json
{
  "clk_freq_mhz": 30,        // 50 → 30 (降低频率要求)
  "core_util": 0.15,         // 0.2 → 0.15 (降低密度)
  "max_fanout": 16,          // 32 → 16 (降低扇出)
  "die_bbox": "0 0 300 300", // 增大 3倍面积
  "core_bbox": "30 30 270 270",
  "target_density": 0.25,    // 0.3 → 0.25
  "max_iterative": 1000      // 2000 → 1000 (减少迭代)
}
```

**预期效果**: 80% 成功率

### 🥈 方案 2: 简化设计

**选项 A: 最小测试**
```bash
./fix_ieda_issue.sh
# 选择 1 - 生成简化版
# 上传 ysyxSoC_simple.v
```

**选项 B: 仅 AI 加速器**
```bash
./fix_ieda_issue.sh
# 选择 2 - 提取 AI 加速器
# 上传 ai_accelerators.v
```

**预期效果**: 95% 成功率

### 🥉 方案 3: 修改本地代码

**修改 Scala 源码** (需要重新生成 Verilog):

```scala
// src/SoC.scala
// 注释掉非必需外设
// val vga = LazyModule(new VGAController)
// val sdram = LazyModule(new SDRAMController)
// val ps2 = LazyModule(new PS2Controller)
```

**预期效果**: 需要重新编译,耗时较长

## 快速决策树

```
iEDA netlist_opt 失败
    |
    ├─ 时间紧急? 
    │   └─ YES → 方案1: 调整参数重试 (5分钟)
    │
    ├─ 需要完整功能?
    │   ├─ YES → 方案1 + 增大面积
    │   └─ NO  → 方案2: 简化设计
    │
    └─ 长期解决?
        └─ 方案3: 修改代码 + 分层综合
```

## 详细步骤

### 步骤 1: 立即重试 (方案1)

1. 打开在线 iEDA 平台
2. 重新上传相同文件
3. 修改参数:
   ```
   时钟频率: 30 MHz
   核心利用率: 0.15
   最大扇出: 16
   芯片尺寸: 300x300
   ```
4. 提交任务
5. 等待结果 (约10-15分钟)

### 步骤 2: 如果仍失败 (方案2)

```bash
cd /opt/github/riscv-ai-accelerator/ecos/ysyxSoC

# 生成简化版本
./fix_ieda_issue.sh
# 选择 1 或 2

# 上传生成的文件到在线 iEDA
# build/ysyxSoC_simple.v 或
# build/ai_accelerators.v
```

### 步骤 3: 验证成功

如果任务完成:
- ✓ 下载 GDS 文件
- ✓ 查看布局布线结果
- ✓ 检查时序报告

## 技术细节

### 为什么降低参数有效?

1. **降低时钟频率** (50→30 MHz)
   - 放宽时序约束
   - 减少缓冲器插入
   - 降低优化难度

2. **降低核心利用率** (0.2→0.15)
   - 更多空白空间
   - 布线更容易
   - 减少拥塞

3. **降低最大扇出** (32→16)
   - 强制插入更多缓冲器
   - 避免单个信号驱动过多负载
   - 符合工具限制

4. **增大芯片面积** (100x100→300x300)
   - 9倍面积
   - 大幅降低密度
   - 给工具更多优化空间

### 为什么简化设计有效?

- 减少模块数量 → 降低复杂度
- 减少信号数量 → 降低扇出
- 保留核心功能 → 验证关键部分

## 预防措施

### 未来设计建议:

1. **分层设计**
   - 不要过度展平
   - 保持模块层次
   - 使用 `hierarchy -keep`

2. **控制扇出**
   - 在 RTL 中插入缓冲器
   - 使用时钟树综合
   - 限制组合逻辑深度

3. **渐进式验证**
   - 先验证小模块
   - 再集成完整系统
   - 逐步增加复杂度

4. **工具兼容性**
   - 测试不同 EDA 工具
   - 准备备选方案
   - 保留简化版本

## 常见问题

**Q: 为什么综合成功但优化失败?**
A: Yosys 综合只生成网表,iEDA 优化需要处理扇出、时序等,要求更高。

**Q: 降低参数会影响性能吗?**
A: 会降低时钟频率,但不影响功能正确性。可以后续优化。

**Q: 简化设计会丢失功能吗?**
A: 是的,但可以先验证核心功能(AI加速器),再逐步添加外设。

**Q: 需要修改 Scala 代码吗?**
A: 不一定。先尝试方案1和2,它们不需要修改源码。

## 相关文件

- `IEDA_FIX_GUIDE.md` - 详细修复指南
- `fix_ieda_issue.sh` - 自动修复脚本
- `build/ysyxSoC_simple.v` - 简化版本
- `build/ai_accelerators.v` - AI加速器版本

## 总结

| 方案 | 难度 | 耗时 | 成功率 | 推荐度 |
|------|------|------|--------|--------|
| 调整参数 | ⭐ | 5分钟 | 80% | ⭐⭐⭐⭐⭐ |
| 简化设计 | ⭐⭐ | 10分钟 | 95% | ⭐⭐⭐⭐ |
| 修改代码 | ⭐⭐⭐⭐ | 1小时+ | 90% | ⭐⭐ |

**建议**: 先用方案1,失败后用方案2,最后才考虑方案3。

---

**快速命令**:
```bash
./fix_ieda_issue.sh  # 交互式修复
cat IEDA_FIX_GUIDE.md  # 详细文档
```
